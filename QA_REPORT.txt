Pre’on v0.22.5 QA
==================
- 45/45 TS/TSX source files parsed with TypeScript transpileModule: PASS.
- Generated Presenter embedded runtime extracted and checked with `node --check`: PASS.
- Presenter timer layout hotfix: `.timer-body` no longer sets `height:100%` on the absolutely positioned widget body; top/bottom constraints now define the real available height.
- Compact container rules preserve the timer action row at small widget sizes; the former <=300 px input row-break was removed so Duration/Deadline controls cannot add a second action row at their minimum heights.
- macOS menu icons: 19/19 PNG files are 36x36 Retina sources; 19 matching SVG source files included.
- package.json / Cargo.toml / tauri.conf.json / APP_VERSION aligned to 0.22.5.
- Native Tauri macOS/Windows build not executed in this environment.

PRE'ON v0.22.4 PRESENTER MODULES + CUSTOM MAC MENU ICONS QA
============================================================

Scope
- Hide/restore Presenter widgets with per-project persistence.
- Keep duration/deadline controls fully visible at the minimum supported widget size.
- Measure timer text against the real free area instead of using the previous character-factor approximation.
- Replace macOS SF/NativeIcon placeholders with bundled Pre'on-style menu glyphs while retaining CheckMenuItem state ticks.
- Keep the dedicated Slideshow icon consistent in toolbar, Layers and Properties.

Validation performed in this environment
- 45 TypeScript/TSX source files checked with the TypeScript parser path: zero TS1xxx syntax diagnostics.
- Generated standalone presenter.html created from a sample project; extracted Presenter runtime passes `node --check`.
- Presenter source contains close controls for every widget, a restore Modules menu, hidden-state persistence, and reset/preset restore logic.
- Smart Next/Previous bypasses note paging when the Notes widget is hidden, so an invisible long note cannot block slide navigation.
- Duration and deadline modules use explicit minimum sizes and fixed 31 px action controls; timer-value font calculation uses canvas text measurement plus available width/height.
- Native macOS menu no longer references `NativeIcon`; custom PNG menu glyphs are bundled under `public/menu-icons`.
- Tauri `image-png` feature enabled for custom menu-image decoding.
- Slideshow maps to the dedicated `slideshow` Icon in Layers and Properties.

Limitations
- Full dependency-aware Vite/Tauri production build could not be run because node_modules are not bundled and dependency installation exceeded the execution time limit.
- Native macOS/Windows build could not be run because Rust/Cargo is unavailable in this environment.

PRE'ON v0.22.3 RICH NOTES + PRESENTER SCALING QA
================================================

Scope
- Rich-text Text Note editing with a custom right-click formatting menu.
- Sanitized rich-note persistence and Presenter rendering while keeping audience exports note-free.
- macOS native menu check states plus supported native menu icons.
- Presenter control overflow hardening and uncapped timer-value scaling to the available widget area.
- Dedicated overlapping-images Slideshow icon.

Validation performed in this environment
- TypeScript/TSX syntax parse across src.
- Standalone strict TypeScript check for the rich-note sanitizer utility.
- Generated Presenter HTML runtime extraction + node --check.
- Portable audience HTML/LAN runtime extraction + node --check where available.
- i18n duplicate-key scan.
- CSS brace balance.
- JSON parse for package/Tauri configs and version consistency checks.
- Shell syntax checks for macOS/web helper scripts.
- Windows console-subsystem guard and base-branch HCR branding scan.
- ZIP integrity check after packaging.

Limitations
- Full dependency-aware Vite/Tauri production build is not run because node_modules are not bundled and dependency installation timed out in this environment.
- Native macOS/Windows build is not run because the Rust/Cargo toolchain is unavailable in this environment.

Pre’on v0.22.2 QA
==================

Scope
- Presenter widget resize: explicit 8-direction handles and persisted layout.
- macOS native menu integration with fallback to the in-app menu.
- Distinct/customizable note background.
- Notes in Single Slide, Continuous and One Slide/Longform editor views.

Validation performed in this environment
- TypeScript/TSX syntax parse across src.
- Presenter HTML runtime extraction + node --check.
- i18n duplicate-key scan.
- CSS brace balance.
- JSON parse for package/Tauri configs.
- Version consistency checks.
- Shell syntax checks for macOS/web helper scripts.
- ZIP integrity check after packaging.

Limitations
- Full Vite/Tauri dependency-aware build is not run because node_modules are not bundled.
- Native macOS/Windows build is not run because Rust/Cargo toolchain is unavailable in this environment.
PRE'ON v0.22.1 FLEXIBLE NOTES + MODULAR PRESENTER QA

Scope
- Resizable portrait Text Note / Visual Note cards in Flow View.
- Long scrollable plain speaker notes without a fixed 16:9 note artboard.
- Freeform draggable/resizable HTML Presenter widgets with saved layouts and presets.
- Simple Standard HTML vs HTML + Presenter export selection.
- One Slide sticky dynamic slide-number end-of-scroll hardening.

Checks completed
OK  All 43 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  Presenter runtime JavaScript passes standalone node --check syntax validation.
OK  Portable audience HTML/LAN runtime JavaScript passes standalone node --check syntax validation.
OK  package.json and Tauri JSON configuration parse successfully.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.22.1.
OK  CSS braces are balanced and the i18n English map contains no duplicate keys.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Windows release entry point remains console-less.
OK  HCR Edition branding is absent from the base Pre'on source.
OK  Presenter Notes remain disabled by default and are enabled from View / Nezet.
OK  New notes default to Text mode at 900 x 1200 (portrait 3:4), independent of the 16:9 slide size.
OK  Text Note is a native scrollable textarea with no maxLength and does not expand the Flow row with text length.
OK  Note width/height can be resized from the Flow card; Visual Note also exposes exact width/height plus 3:4 and 4:3 presets.
OK  Visual Note keeps the regular Pre'on canvas/editing element workflow and preserves its own elements/assets.
OK  v0.22.0 simple/one-text-box note boards migrate to Text Note while complex note boards remain Visual Note.
OK  Changing active slides cannot leave the editor in Visual Note canvas mode when the target slide is a Text Note.
OK  Presenter has independent draggable/resizable Current, Next, Notes, Controls, Clock, Duration and Deadline widgets.
OK  Presenter layout is stored per project and Standard / Notes Focus / Timer Focus presets remain inside the viewport.
OK  Text Note presenter view is long-scrollable; smart next/previous pages notes before changing the audience slide.
OK  Presentation menu exposes only Standard HTML and HTML + Presenter as the export-mode choice.
OK  Standard desktop export writes a single audience index.html and excludes Presenter/note assets and note-only font preparation.
OK  HTML + Presenter adds presentation.html and presenter.html; audience payload still strips notes/notesBoard.
OK  One Slide dynamic slide-number probe is clamped to authored section bounds and HTML fallback resolves to the final slide instead of 1.
OK  v0.22.0 presenter transport/timers/video Start-End, v0.21.2 Slideshow and earlier functionality remain wired.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- cargo/rustc are not installed in this execution environment, so a native Rust/Tauri compile check could not be run here.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should be produced with the included target-platform build scripts.

PRE'ON v0.22.0 PRESENTER WORKFLOW QA

Scope
- Presenter Notes Board redesigned as paired artboards in the continuous editor canvas.
- Automatic readable starter text box for notes.
- Separate portable presentation.html + presenter.html export workflow.
- Presenter smart notes paging, timers and basic audience/media controls.
- One Slide sticky dynamic slide-number end-of-scroll fix.
- Video start/end/default-control migration normalization.

Checks completed
OK  All 43 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  Generated Presenter runtime JavaScript passes standalone node --check syntax validation.
OK  Portable audience HTML/LAN runtime JavaScript passes standalone node --check syntax validation.
OK  A generated standalone presenter.html smoke document was created successfully from a sample Presenter project.
OK  package.json and Tauri JSON configuration parse successfully.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.22.0.
OK  CSS braces are balanced and the i18n English map contains no duplicate keys.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Windows release entry point remains console-less.
OK  HCR Edition branding is absent from the base Pre'on source.
OK  Presenter Notes Board remains disabled by default and View activation switches into the paired continuous workflow.
OK  Notes flow uses project-width slide + project-width notes artboards with a dedicated gap, row-aligned per slide.
OK  Notes editing still targets the normal editor container/tooling rather than a separate limited text editor.
OK  Newly created notes boards contain an empty 38 px readable starter textbox; pre-v0.22 empty boards receive it once during migration.
OK  Legacy plain speaker notes still migrate into a visual notes text element.
OK  Audience projectForOutput strips both legacy notes and notesBoard.
OK  Normal HTML export includes notes only in presenter.html; LAN export disables Presenter payload/notes assets.
OK  Desktop HTML bundle writes presentation.html, presenter.html and compatibility index.html.
OK  Presenter/Audience command channel supports next, previous, goto, black screen, media toggle and fullscreen request.
OK  Presenter UI includes current/next preview, visual Notes Board, smart notes-first paging, notes zoom, exact clock, duration budget/countdown and deadline timer.
OK  Video creation keeps controls=false by default; project migration normalizes controls/startTime/endTime.
OK  One Slide dynamic slide-number context falls back to the final viewport section and stays dynamic after the sticky pin range instead of reverting to slide 1.
OK  v0.21.2 Slideshow, v0.21.1 context-menu and earlier productivity/startup fixes remain in the source.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- cargo/rustc are not installed in this execution environment, so a native Rust/Tauri compile check could not be run here.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should be produced with the included target-platform build scripts.
- Browser fullscreen requests remain subject to the browser/OS user-activation policy; Presenter communication itself does not require a separate server.

PRE'ON v0.21.3 PRESENTER NOTES BOARD + ONE SLIDE NUMBER QA

Scope
- Optional visual Presenter Notes Board enabled from View.
- Reuse of the regular editor/tooling on the active notes board.
- Legacy plain speaker-note migration and audience-export privacy.
- Sticky dynamic slide-number update in One Slide / Longform Present + HTML/LAN.

Checks completed
OK  All 43 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  Portable HTML/LAN presentation runtime passes standalone JavaScript syntax validation.
OK  package.json and Tauri JSON configuration parse successfully.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.21.3.
OK  CSS braces are balanced and i18n English map contains no duplicate keys.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Windows release entry point remains console-less.
OK  Presenter Notes Board defaults to hidden and is toggled from View / Nezet.
OK  Notes board is a per-slide project container with its own elements, background, guides and grid.
OK  Existing editor add/update/delete/duplicate/reorder/align flows target the notes board while notes editing is active.
OK  Ruler-created, moved and removed guides target the notes board while notes editing is active.
OK  Slide duplication remaps notes-board element/group IDs.
OK  Legacy slide.notes text migrates once into a visual text element when a project has no notesBoard yet.
OK  Assets referenced only by a notes board are counted as in-use by the Assets panel.
OK  HTML/LAN audience payload strips legacy notes and notesBoard; PDF/Present scenes do not render notes-board content.
OK  Longform Present dynamic text resolves pinned dynamic slide fields against the viewport's active section.
OK  HTML/LAN longform runtime updates pinned dynamic text on scroll and bypasses static outline rendering for dynamic fields.
OK  v0.21.2 Slideshow and earlier context-menu/productivity/startup fixes remain in the source.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should be produced with the included target-platform build scripts.

PRE'ON v0.21.2 SLIDESHOW QA

Scope
- Multi-image Slideshow element.
- Automatic or click-to-start playback.
- Cut/Fade transitions, timing, loop and click play/pause.
- Editor preview, frame/mask settings, Present + HTML/LAN integration.
- Asset usage and legacy project migration.

Checks completed
OK  All 42 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  Portable HTML/LAN presentation runtime passes standalone JavaScript syntax validation.
OK  CSS braces are balanced and i18n contains no duplicate translation keys.
OK  Slideshow is wired into element types, factory, project migration, editor canvas, Scene, Present mode, Layers, Assets, toolbar and context-menu Add submenu.
OK  Multiple slideshows can exist because each element owns independent playback state and asset ordering.
OK  Slideshow images can be reordered by drag & drop or fallback order buttons.
OK  Multi-file drop onto an existing Slideshow appends all compatible image files.
OK  Static thumbnail/PDF rendering uses the first Slideshow frame.
OK  HTML/LAN runtime excludes interactive Slideshow clicks from slide-advance clicks and supports visibility-aware Longform autoplay.
OK  v0.20.9 text overflow/startup guards remain present.
OK  Windows release entry point remains console-less.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.21.2.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should be produced with the included target-platform build scripts.

PRE'ON v0.21.1 DYNAMIC CONTEXT MENU QA

Scope
- Dynamic object-aware custom right-click menu.
- Separate configurable context-menu profiles in Workspace settings.
- Reorder / remove / separator / submenu / reset support.
- Legacy workspace migration.

Checks completed
OK  All 42 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  package.json and Tauri JSON configurations parse correctly.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.21.1.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Windows release entry point remains console-less.
OK  Context-menu command registry contains unique IDs and all default profile references resolve.
OK  Legacy workspace configs receive default context-menu profiles.
OK  Explicitly empty context-menu profiles remain empty.
OK  Context menu profiles distinguish canvas / text / shape / media / multi-select / slide thumbnail.
OK  Text caret/content editing keeps the native text context menu instead of applying whole-object commands.
OK  v0.21.0 productivity tools and v0.20.9 startup/overflow guard remain in place.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should be produced with the included target-platform build scripts.

PRE'ON v0.21.0 PRODUCTIVITY TOOLS QA

Scope
- Color eyedropper + text format eyedropper.
- Multi-select text formatting with mixed-value handling.
- Shared Effects inspector: Drop Shadow, Blur, Glow.
- Copy/Paste Style.
- Select Same.
- Project-wide Replace Font.

Checks completed
OK  All 40 TypeScript / TSX source files parse with zero syntax diagnostics.
OK  package.json and all Tauri JSON configurations parse correctly.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.21.0.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Portable HTML presentation runtime passes standalone JavaScript syntax validation after Effects integration.
OK  Effects are normalized for legacy project elements during migration.
OK  Editor and Static Scene both render the shared element Effects filter.
OK  HTML/LAN runtime renders the same blur / drop-shadow / glow filter chain.
OK  Style snapshot logic does not copy text content or textbox geometry; typography/style properties are transferred.
OK  Multi-text formatting uses mixed values and only applies the property explicitly changed by the user.
OK  Select Same is scoped to the active editable container; Replace Font is project-wide across slides, masters and Text Styles.
OK  v0.20.9 overflow/startup guard remains present.

Environment limitation
- node_modules are not bundled with this source archive, so the dependency-aware Vite/Tauri production build was not executed in this environment.
- Native macOS .app/.dmg and Windows NSIS/.exe builds should still be produced with the included target-platform build scripts.

PRE'ON v0.20.9 HOTFIX QA

- Startup crash root cause fixed: ResizeObserver no longer references an undefined `node`.
- Guard added around overflow measurement so a measurement failure cannot crash the editor.
- Source syntax checks rerun after patch.
- Version consistency checked across package.json, Cargo.toml, tauri.conf.json and src/utils.ts.

Pre'on v0.20.8 QA REPORT

Scope
- Fix false positive text overflow indicators.
- Preserve wrapping when switching Box / Scale Text resize modes.
- Add canvas-side resize mode toggle for selected text boxes.
- Move Text Overview directly before Assets.
- Restyle Text Overview as a continuous proofing view.

Checks completed
OK  TypeScript / TSX syntax transpile check for every source file.
OK  package.json, Tauri JSON configuration files parse correctly.
OK  package / Cargo / Tauri / APP_VERSION consistency = 0.20.8.
OK  macOS shell build/launch scripts pass bash syntax checks.
OK  Windows release entry point remains console-less in release builds.
OK  Text resize mode switching does not modify width or height.
OK  Text editing / Find & Change / font changes / text style changes no longer auto-hug Scale Text boxes.
OK  Scale Text mode exposes corner handles only and scales text spacing metrics proportionally.
OK  Canvas-side Box / Text resize toggle is present at the right-center of a selected text box.
OK  Right panel order is Properties / Layers / Text Overview / Assets.
OK  Text Overview uses continuous rows with auto-height text fields and subtle dividers.
OK  Canvas and Text Overview use the same intrinsic overflow measurement with subpixel tolerance.

Environment limitation
- node_modules are not bundled with the source package in this environment.
- A dependency-aware Vite/Tauri production build was therefore not executed here.
- Native macOS .app/.dmg and Windows NSIS/.exe builds must be produced on their target build environments.
