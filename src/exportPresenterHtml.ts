export function buildPresenterDocument(title: string, projectJson: string, fontCss: string): string {
  return `<!doctype html>
<html lang="hu">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>${title} · Presenter</title>
<style>
${fontCss}
:root{font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:dark;--panel:#181a1e;--panel2:#202329;--border:#ffffff22;--muted:#9ca2ab;--accent:#819b7e;--text:#f3f4f5}
*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:#0b0d10;color:var(--text)}button,input{font:inherit}button{border:1px solid var(--border);background:#252930;color:var(--text);border-radius:8px;padding:7px 10px;cursor:pointer}button:hover{background:#30353d}button.primary{background:#71886f;border-color:#93a990;color:#fff}button.active{outline:2px solid #d2a36e}.app{height:100%;display:flex;flex-direction:column}.topbar{height:48px;flex:0 0 48px;display:flex;align-items:center;gap:7px;padding:6px 10px;background:#111318;border-bottom:1px solid var(--border);overflow:visible}.modules-wrap{position:relative;flex:0 0 auto}.modules-menu{position:fixed;z-index:1000;min-width:190px;padding:6px;background:#1a1d22;border:1px solid var(--border);border-radius:10px;box-shadow:0 16px 44px #0009;display:none}.modules-menu.open{display:grid;gap:3px}.modules-menu button{text-align:left;width:100%;border:0;background:transparent;padding:7px 9px}.modules-menu button:hover{background:#2a2e35}.modules-menu .is-visible::before{content:"✓";display:inline-block;width:18px;color:#a9c0a7}.modules-menu .is-hidden::before{content:"＋";display:inline-block;width:18px;color:#9ca2ab}.brand{font-weight:720;margin-right:4px}.status{font-size:11px;color:var(--muted);margin-right:auto}.preset-label{font-size:10px;color:var(--muted)}.workspace{position:relative;flex:1;min-height:0;overflow:hidden;background:radial-gradient(circle at 25% 20%,#1a1d2238,transparent 38%),#0d0f12}.widget{position:absolute;min-width:180px;min-height:120px;background:var(--panel);border:1px solid var(--border);border-radius:13px;box-shadow:0 14px 38px #0006;overflow:hidden;container-type:size}.widget-resizer{position:absolute;z-index:12;touch-action:none}.widget-resizer[data-resize="n"],.widget-resizer[data-resize="s"]{left:18px;right:18px;height:14px;cursor:ns-resize}.widget-resizer[data-resize="n"]{top:0}.widget-resizer[data-resize="s"]{bottom:0}.widget-resizer[data-resize="e"],.widget-resizer[data-resize="w"]{top:18px;bottom:18px;width:14px;cursor:ew-resize}.widget-resizer[data-resize="e"]{right:0}.widget-resizer[data-resize="w"]{left:0}.widget-resizer[data-resize="ne"],.widget-resizer[data-resize="nw"],.widget-resizer[data-resize="se"],.widget-resizer[data-resize="sw"]{width:26px;height:26px;background:radial-gradient(circle at center,#aeb5bd 0 2px,transparent 3px);opacity:.34}.widget:hover .widget-resizer[data-resize="ne"],.widget:hover .widget-resizer[data-resize="nw"],.widget:hover .widget-resizer[data-resize="se"],.widget:hover .widget-resizer[data-resize="sw"],.widget.resizing .widget-resizer{opacity:.8}.widget-resizer[data-resize="ne"]{right:0;top:0;cursor:nesw-resize}.widget-resizer[data-resize="nw"]{left:0;top:0;cursor:nwse-resize}.widget-resizer[data-resize="se"]{right:0;bottom:0;cursor:nwse-resize}.widget-resizer[data-resize="sw"]{left:0;bottom:0;cursor:nesw-resize}.widget-resizer[data-resize="se"]::after{content:"";position:absolute;right:5px;bottom:5px;width:14px;height:14px;opacity:.78;background:linear-gradient(135deg,transparent 0 38%,#9aa1aa 39% 49%,transparent 50% 61%,#9aa1aa 62% 72%,transparent 73%)}.widget.resizing{border-color:#819b7ecc;box-shadow:0 16px 44px #0008,0 0 0 1px #819b7e88}.widget:focus-within,.widget.dragging{border-color:#819b7e88;box-shadow:0 16px 44px #0008,0 0 0 1px #819b7e55}.widget-head{height:34px;display:flex;align-items:center;gap:7px;padding:0 9px;border-bottom:1px solid var(--border);background:#202329;cursor:grab;user-select:none;font-size:11px;color:#d9dde2}.widget-head:active{cursor:grabbing}.widget-grip{opacity:.48;letter-spacing:-2px}.widget-head .meta{margin-left:auto;color:var(--muted);font-variant-numeric:tabular-nums;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.widget-close{flex:0 0 24px;width:24px;height:24px;min-height:24px;padding:0;border:0;border-radius:6px;background:transparent;color:#9198a1;font-size:17px;line-height:1;display:grid;place-items:center;cursor:pointer}.widget-close:hover{background:#ffffff10;color:#fff}.widget[hidden]{display:none!important}.widget-body{position:absolute;left:0;right:0;top:34px;bottom:0;min-height:0;overflow:hidden}.preview-host{width:100%;height:100%;overflow:hidden;background:#090a0c;display:grid;place-items:center;padding:8px}.scaled-shell{position:relative;margin:auto}.artboard{position:absolute;left:0;top:0;transform-origin:top left;overflow:hidden;background:#fff;box-shadow:0 12px 34px #0008}.art-el{position:absolute;transform-origin:center center}.art-text{width:100%;height:100%;display:flex;white-space:pre-wrap;overflow:visible}.art-media{width:100%;height:100%;display:block}.placeholder{width:100%;height:100%;display:grid;place-items:center;background:#d8dadd;color:#555;font:30px/1.2 Inter,sans-serif}.notes-scroll{width:100%;height:100%;overflow:auto;scroll-behavior:smooth;background:#101216;scrollbar-gutter:stable}.plain-notes,.rich-notes{min-height:100%;padding:28px 34px 70px;color:#f0f1f2;font:clamp(20px,5cqw,42px)/1.46 Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;white-space:pre-wrap;overflow-wrap:anywhere}.plain-notes.empty{color:#747a82;font-style:italic}.rich-notes p{margin:.2em 0 .65em}.rich-notes h1,.rich-notes h2,.rich-notes h3{margin:.3em 0 .45em;line-height:1.1}.rich-notes h1{font-size:1.75em}.rich-notes h2{font-size:1.42em}.rich-notes h3{font-size:1.18em}.rich-notes blockquote{margin:.45em 0;padding:.1em 0 .1em .75em;border-left:.12em solid currentColor;opacity:.82}.rich-notes ul,.rich-notes ol{padding-left:1.35em}.rich-notes mark{background:#fff19b;color:inherit}.notes-artboard-shell{position:relative;margin:18px auto 52px}.notes-page{padding:0 7px;color:var(--muted);font-size:10px}.timer-body{padding:10px;display:flex;flex-direction:column;gap:6px;height:auto;min-height:0;overflow:hidden}.timer-value{font-size:34px;font-weight:690;line-height:.9;letter-spacing:-.045em;white-space:nowrap;font-variant-numeric:tabular-nums;flex:1 1 auto;min-height:20px;min-width:0;display:flex;align-items:center;justify-content:center;overflow:hidden;text-align:center}.timer-sub{font-size:11px;color:var(--muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;flex:0 0 auto;min-height:14px}.timer-actions{display:grid;gap:5px;align-items:stretch;flex:0 0 auto;max-width:100%;min-width:0;position:relative;z-index:2}.timer-actions input,.timer-actions button{min-width:0;width:100%;height:31px;min-height:31px}.timer-actions input{background:#101216;color:#fff;border:1px solid var(--border);border-radius:7px;padding:5px 6px}.timer-actions button{padding:5px 6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}[data-widget="duration"] .timer-actions{grid-template-columns:minmax(0,1.18fr) minmax(0,1fr) minmax(0,1fr)}[data-widget="deadline"] .timer-actions{grid-template-columns:minmax(0,1.35fr) minmax(0,1fr)}@container (max-height: 210px){.timer-body{padding:7px;gap:4px}.timer-actions input,.timer-actions button{height:28px;min-height:28px;padding:3px 5px}.timer-sub{min-height:12px;font-size:9px}}@container (max-width: 240px){.timer-actions{gap:4px}.timer-actions input,.timer-actions button{padding-left:4px;padding-right:4px;font-size:10px}}.control-grid{height:100%;padding:10px;display:grid;grid-template-columns:repeat(auto-fit,minmax(min(132px,100%),1fr));grid-auto-rows:minmax(34px,1fr);gap:7px;overflow:auto;align-content:stretch}.control-grid button{min-width:0;min-height:34px;white-space:normal;overflow-wrap:anywhere}.counter{font-variant-numeric:tabular-nums}.hint{font-size:10px;color:var(--muted)}.node-badge{display:inline-flex;align-items:center;padding:2px 6px;border-radius:999px;background:#819b7e2d;color:#a9c0a7;font-size:9px}.no-select{user-select:none}
</style>
</head>
<body>
<div class="app">
  <div class="topbar">
    <span class="brand">Pre'on Presenter</span><span id="connection" class="status">Nincs megnyitott prezentáció</span>
    <button id="openPresentation" class="primary">Prezentáció megnyitása</button>
    <span class="preset-label">Elrendezés:</span><button data-preset="standard">Standard</button><button data-preset="notes">Notes Focus</button><button data-preset="timer">Timer Focus</button><button id="resetLayout">Reset</button>
    <span class="modules-wrap"><button id="modulesButton">Modulok ▾</button><span id="modulesMenu" class="modules-menu" role="menu"></span></span>
  </div>
  <div id="workspace" class="workspace">
    <section class="widget" data-widget="current"><div class="widget-head"><span class="widget-grip">⠿</span><span>Aktuális dia</span><span id="currentName" class="meta"></span></div><div id="currentPreview" class="widget-body preview-host"></div></section>
    <section class="widget" data-widget="next"><div class="widget-head"><span class="widget-grip">⠿</span><span>Következő dia</span><span id="nextName" class="meta"></span></div><div id="nextPreview" class="widget-body preview-host"></div></section>
    <section class="widget" data-widget="notes"><div class="widget-head"><span class="widget-grip">⠿</span><span>Jegyzet</span><span id="notesMode" class="node-badge">TEXT</span><span id="notesPage" class="meta notes-page">1 / 1</span></div><div id="notesHost" class="widget-body notes-scroll"></div></section>
    <section class="widget" data-widget="controls"><div class="widget-head"><span class="widget-grip">⠿</span><span>Vezérlés</span><span id="counter" class="meta counter">1 / 1</span></div><div class="widget-body control-grid"><button id="prev">‹ Előző</button><button id="nextButton">Következő ›</button><button id="media">▶︎ / ❚❚ média</button><button id="black">Fekete kép</button><button id="fullscreen">Közönség fullscreen</button><button id="presenterFullscreen">Presenter fullscreen</button></div></section>
    <section class="widget" data-widget="clock"><div class="widget-head"><span class="widget-grip">⠿</span><span>Pontos idő</span></div><div class="widget-body timer-body"><div id="clockValue" class="timer-value">00:00:00</div><div class="timer-sub">Aktuális helyi idő</div></div></section>
    <section class="widget" data-widget="duration"><div class="widget-head"><span class="widget-grip">⠿</span><span>Időkeret</span></div><div class="widget-body timer-body"><div id="durationValue" class="timer-value">45:00</div><div id="durationSub" class="timer-sub">0:00 eltelt</div><div class="timer-actions"><input id="durationMinutes" type="number" min="1" max="1440" value="45"><button id="durationStart">Start</button><button id="durationReset">Reset</button></div></div></section>
    <section class="widget" data-widget="deadline"><div class="widget-head"><span class="widget-grip">⠿</span><span>Befejezés eddig</span></div><div class="widget-body timer-body"><div id="deadlineValue" class="timer-value">--:--</div><div id="deadlineSub" class="timer-sub">Nincs határidő</div><div class="timer-actions"><input id="deadlineTime" type="time"><button id="deadlineClear">Törlés</button></div></div></section>
  </div>
</div>
<script id="presenter-project" type="application/json">${projectJson}</script>
<script>
(()=>{
const project=JSON.parse(document.getElementById('presenter-project').textContent);const session=(crypto.randomUUID?crypto.randomUUID():String(Date.now())+Math.random().toString(16).slice(2));let presentationWindow=null,channel=null,currentIndex=0,connected=false,black=false,lastRenderedIndex=-1;
try{channel=new BroadcastChannel('preon-presenter-'+session)}catch{}
const byId=id=>document.getElementById(id),workspace=byId('workspace'),notesHost=byId('notesHost'),widgets=[...document.querySelectorAll('.widget')],modulesButton=byId('modulesButton'),modulesMenu=byId('modulesMenu');
const layoutKey='preon-presenter-layout-'+(project.id||'default'),hiddenKey='preon-presenter-hidden-'+(project.id||'default');
const widgetNames={current:'Aktuális dia',next:'Következő dia',notes:'Jegyzet',controls:'Vezérlés',clock:'Pontos idő',duration:'Időkeret',deadline:'Befejezés eddig'};
function asset(id){const a=project.assets.find(x=>x.id===id);return a?(a.dataUrl||a.path||''):''}
function noteInk(color){const raw=String(color||'').replace('#','');const hex=raw.length===3?raw.split('').map(x=>x+x).join(''):raw;if(!/^[0-9a-f]{6}$/i.test(hex))return'#24262a';const r=parseInt(hex.slice(0,2),16),g=parseInt(hex.slice(2,4),16),b=parseInt(hex.slice(4,6),16);return((.2126*r+.7152*g+.0722*b)/255)<.48?'#f4f5f2':'#24262a'}
function masterChain(id){const by=new Map(project.masters.map(m=>[m.id,m])),chain=[],seen=new Set();let cur=id?by.get(id):null;while(cur&&!seen.has(cur.id)){chain.unshift(cur);seen.add(cur.id);cur=cur.parentMasterId?by.get(cur.parentMasterId):null}return chain}
function masterElements(slide){const overridden=new Set((slide.elements||[]).map(e=>e.masterSourceId).filter(Boolean));return masterChain(slide.masterId).flatMap(m=>m.elements||[]).filter(e=>!overridden.has(e.id))}
function masterBackground(slide){let bg={inherit:false,color:'#fff',type:'color',fit:'cover'};masterChain(slide.masterId).forEach(m=>{if(!m.background?.inherit||!m.parentMasterId)bg=m.background||bg});return bg}
function resolvedBackground(slide){return slide.background?.inherit&&slide.masterId?masterBackground(slide):(slide.background||{color:'#fff',type:'color',fit:'cover'})}
function shapeClip(kind){if(kind==='triangle')return'polygon(50% 0,100% 100%,0 100%)';if(kind==='star')return'polygon(50% 0,61% 35%,98% 35%,68% 57%,79% 94%,50% 72%,21% 94%,32% 57%,2% 35%,39% 35%)';if(kind==='polygon')return'polygon(25% 6.7%,75% 6.7%,100% 50%,75% 93.3%,25% 93.3%,0 50%)';if(kind==='arrow')return'polygon(0% 32%,62% 32%,62% 8%,100% 50%,62% 92%,62% 68%,0% 68%)';return''}
function dynText(el,slide){if(!el.dynamicField||el.dynamicField==='none')return el.text||'';const idx=Math.max(0,project.slides.findIndex(s=>s.id===slide?.id))+1,total=project.slides.length,digits=el.numberFormat==='001'?3:el.numberFormat==='01'?2:1,fmt=n=>digits>1?String(n).padStart(digits,'0'):String(n);let value=fmt(idx);if(el.dynamicField==='total-slides')value=fmt(total);if(el.dynamicField==='slide-number-total')value=fmt(idx)+' / '+fmt(total);return(el.dynamicPrefix||'')+value+(el.dynamicSuffix||'')}
function visual(el,slide){let n;if(el.type==='text'){n=document.createElement('div');n.className='art-text';n.textContent=dynText(el,slide);n.style.fontFamily=el.fontFamily;n.style.fontSize=(el.fontSize||30)+'px';n.style.fontWeight=String(el.fontWeight||400);n.style.fontStyle=el.fontStyle||'normal';n.style.textDecoration=[el.textDecoration==='underline'?'underline':'',el.strikethrough?'line-through':''].filter(Boolean).join(' ')||'none';n.style.textTransform=el.textTransform==='uppercase'?'uppercase':'none';n.style.fontVariantCaps=el.fontVariantCaps==='small-caps'?'small-caps':'normal';n.style.lineHeight=el.lineHeightAuto?'normal':String(el.lineHeight||1.2);n.style.letterSpacing=el.letterSpacingAuto?'normal':(el.letterSpacing||0)+'px';n.style.color=el.color||'#17181b';n.style.textAlign=el.textAlign||'left';n.style.background=el.background||'transparent';n.style.padding=(el.padding||0)+'px';n.style.justifyContent=el.verticalAlign==='middle'?'center':el.verticalAlign==='bottom'?'flex-end':'flex-start';n.style.flexDirection='column';return n}
if(el.type==='shape'){n=document.createElement('div');n.style.width='100%';n.style.height='100%';n.style.border=el.strokeWidth>0?el.strokeWidth+'px solid '+el.stroke:'none';n.style.borderRadius=el.shape==='ellipse'?'50%':((el.shape==='rect'||el.shape==='rounded-rect')?(el.radius||0)+'px':'0');const clip=shapeClip(el.shape);if(clip)n.style.clipPath=clip;if(el.fillType==='gradient')n.style.background='linear-gradient('+(el.gradientAngle||0)+'deg,'+(el.gradientFrom||el.fill)+','+(el.gradientTo||el.fill)+')';else if((el.fillType==='image'||el.fillType==='video')&&asset(el.fillAssetId)){const im=document.createElement('img');im.className='art-media';im.src=asset(el.fillAssetId);im.style.objectFit=el.fillFit||'cover';n.append(im)}else n.style.background=el.fill||'#ddd';return n}
const media=document.createElement('img');media.className='art-media';if(el.type==='image')media.src=asset(el.assetId);else if(el.type==='slideshow')media.src=asset((el.assetIds||[])[0]);else if(el.type==='pdf')media.src=el.renderedDataUrl||'';else if(el.type==='video')media.src=el.posterDataUrl||'';else if(el.type==='model3d')media.src=el.posterDataUrl||'';if(media.src){media.style.objectFit=el.fit||'contain';media.style.borderRadius=(el.radius||0)+'px';return media}n=document.createElement('div');n.className='placeholder';n.textContent=el.type==='video'?'▶ Video':el.type==='model3d'?'3D':el.type==='web'?'Web':'Média';return n}
function addElements(art,elements,slide){(elements||[]).filter(e=>e.visible!==false).forEach(el=>{const w=document.createElement('div');w.className='art-el';w.style.left=(el.x||0)+'px';w.style.top=(el.y||0)+'px';w.style.width=(el.width||1)+'px';w.style.height=(el.height||1)+'px';w.style.opacity=String(el.opacity??1);w.style.transform='rotate('+(el.rotation||0)+'deg)';w.append(visual(el,slide));art.append(w)})}
function background(art,bg){art.style.backgroundColor=bg?.color||'#fff';if(bg?.type==='image'&&asset(bg.assetId)){const im=document.createElement('img');im.className='art-media';im.src=asset(bg.assetId);im.style.position='absolute';im.style.inset='0';im.style.objectFit=bg.fit||'cover';art.append(im)}}
function mountSlide(host,slide){host.replaceChildren();if(!slide)return;const shell=document.createElement('div');shell.className='scaled-shell';const art=document.createElement('div');art.className='artboard';art.style.width=project.width+'px';art.style.height=project.height+'px';background(art,resolvedBackground(slide));addElements(art,[...masterElements(slide),...(slide.elements||[])],slide);shell.append(art);host.append(shell);const fit=()=>{const aw=Math.max(1,host.clientWidth-16),ah=Math.max(1,host.clientHeight-16),scale=Math.min(aw/project.width,ah/project.height);art.style.transform='scale('+scale+')';shell.style.width=(project.width*scale)+'px';shell.style.height=(project.height*scale)+'px'};fit();requestAnimationFrame(fit)}
function mountNotes(slide){notesHost.replaceChildren();const board=slide?.notesBoard;const mode=board?.mode||'text',paper=board?.background?.color||'#e9efe6';byId('notesMode').textContent=mode==='visual'?'VISUAL':'TEXT';if(!slide)return;if(mode==='text'){notesHost.style.background=paper;const hasRich=Boolean((slide.notesHtml||'').trim()),hasPlain=Boolean((slide.notes||'').trim());const text=document.createElement('div');text.className=hasRich?'rich-notes':'plain-notes'+(hasPlain?'':' empty');text.style.color=noteInk(paper);if(hasRich)text.innerHTML=slide.notesHtml;else text.textContent=hasPlain?slide.notes:'Nincs jegyzet ehhez a diához.';notesHost.append(text);updateNotesPage();return}notesHost.style.background='#101216';const width=board?.width||900,height=board?.height||1200;const shell=document.createElement('div');shell.className='notes-artboard-shell';const art=document.createElement('div');art.className='artboard';art.style.width=width+'px';art.style.height=height+'px';background(art,board?.background||{color:'#e9efe6'});addElements(art,board?.elements||[],slide);shell.append(art);notesHost.append(shell);const fit=()=>{const scale=Math.max(.1,(notesHost.clientWidth-36)/width);art.style.transform='scale('+scale+')';shell.style.width=(width*scale)+'px';shell.style.height=(height*scale)+'px'};fit();requestAnimationFrame(()=>{fit();updateNotesPage()})}
function updateNotesPage(){const pages=Math.max(1,Math.ceil(notesHost.scrollHeight/Math.max(1,notesHost.clientHeight))),page=Math.min(pages,Math.floor(notesHost.scrollTop/Math.max(1,notesHost.clientHeight*.78))+1);byId('notesPage').textContent=page+' / '+pages}
notesHost.addEventListener('scroll',updateNotesPage,{passive:true});
function renderAll(force=false){const slide=project.slides[currentIndex],next=project.slides[currentIndex+1];if(force||lastRenderedIndex!==currentIndex){lastRenderedIndex=currentIndex;byId('currentName').textContent=slide?.name||'';byId('nextName').textContent=next?.name||'—';byId('counter').textContent=(currentIndex+1)+' / '+project.slides.length;mountSlide(byId('currentPreview'),slide);mountSlide(byId('nextPreview'),next);mountNotes(slide);notesHost.scrollTop=0;updateNotesPage()}}
function send(command,extra={}){const msg={source:'preon-presenter',command,...extra};try{presentationWindow?.postMessage(msg,'*')}catch{}try{channel?.postMessage(msg)}catch{}}
function receive(event){const data=event&&event.data!==undefined?event.data:event;if(!data||data.source!=='preon-presentation')return;connected=true;byId('connection').textContent='Kapcsolódva a prezentációhoz';const st=data.state;if(st&&Number.isFinite(Number(st.index))){currentIndex=Math.max(0,Math.min(project.slides.length-1,Number(st.index)));black=!!st.black;byId('black').classList.toggle('active',black);renderAll()}}
addEventListener('message',receive);if(channel)channel.onmessage=receive;
byId('openPresentation').onclick=()=>{const url='presentation.html?controller='+encodeURIComponent(session);presentationWindow=window.open(url,'preon-presentation');byId('connection').textContent='Kapcsolódás…';setTimeout(()=>send('goto',{index:currentIndex}),450)};
function smartNext(){const notesWidget=document.querySelector('[data-widget="notes"]');if(!notesWidget?.hidden){const remaining=notesHost.scrollHeight-notesHost.clientHeight-notesHost.scrollTop;if(remaining>8){notesHost.scrollBy({top:Math.max(180,notesHost.clientHeight*.78),behavior:'smooth'});return}}send('next')}
function smartPrev(){const notesWidget=document.querySelector('[data-widget="notes"]');if(!notesWidget?.hidden&&notesHost.scrollTop>8){notesHost.scrollBy({top:-Math.max(180,notesHost.clientHeight*.78),behavior:'smooth'});return}send('prev')}
byId('nextButton').onclick=smartNext;byId('prev').onclick=smartPrev;byId('media').onclick=()=>send('media-toggle');byId('fullscreen').onclick=()=>send('fullscreen');byId('presenterFullscreen').onclick=()=>document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen?.();byId('black').onclick=()=>{black=!black;byId('black').classList.toggle('active',black);send('black',{value:black})};
addEventListener('keydown',e=>{if(e.target instanceof HTMLInputElement||e.target instanceof HTMLTextAreaElement)return;if(['ArrowRight','ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();smartNext()}else if(['ArrowLeft','ArrowUp','PageUp'].includes(e.key)){e.preventDefault();smartPrev()}else if(e.key==='b'||e.key==='B')byId('black').click();else if(e.key==='m'||e.key==='M')byId('media').click()});
// Freeform node workspace. Every module can be moved and resized from all four sides/corners.
function widgetMinSize(id){if(id==='duration')return{w:220,h:126};if(id==='deadline')return{w:205,h:126};if(id==='controls')return{w:220,h:145};return{w:180,h:120}}
function setRect(id,x,y,w,h){const node=document.querySelector('[data-widget="'+id+'"]');if(!node)return;const min=widgetMinSize(id),maxW=Math.max(min.w,workspace.clientWidth),maxH=Math.max(min.h,workspace.clientHeight),width=Math.min(maxW,Math.max(min.w,w)),height=Math.min(maxH,Math.max(min.h,h));node.style.left=Math.min(Math.max(0,x),Math.max(0,workspace.clientWidth-width))+'px';node.style.top=Math.min(Math.max(0,y),Math.max(0,workspace.clientHeight-height))+'px';node.style.width=width+'px';node.style.height=height+'px'}
function currentLayout(){const out={};widgets.forEach(node=>{const id=node.dataset.widget;out[id]={x:parseFloat(node.style.left)||node.offsetLeft||0,y:parseFloat(node.style.top)||node.offsetTop||0,w:parseFloat(node.style.width)||node.offsetWidth||widgetMinSize(id).w,h:parseFloat(node.style.height)||node.offsetHeight||widgetMinSize(id).h}});return out}
function hiddenWidgets(){return widgets.filter(node=>node.hidden).map(node=>node.dataset.widget)}
let saveTimer=0;function saveLayout(){clearTimeout(saveTimer);saveTimer=setTimeout(()=>{try{localStorage.setItem(layoutKey,JSON.stringify(currentLayout()));localStorage.setItem(hiddenKey,JSON.stringify(hiddenWidgets()))}catch{}},120)}
function refreshModuleMenu(){modulesMenu.replaceChildren();widgets.forEach(node=>{const id=node.dataset.widget,button=document.createElement('button');button.type='button';button.dataset.module=id;button.className=node.hidden?'is-hidden':'is-visible';button.textContent=widgetNames[id]||id;button.onclick=()=>{setWidgetVisible(id,node.hidden);modulesMenu.classList.remove('open')};modulesMenu.append(button)})}
function setWidgetVisible(id,visible,persist=true){const node=document.querySelector('[data-widget="'+id+'"]');if(!node)return;node.hidden=!visible;if(visible){raise(node);requestAnimationFrame(()=>{fitWidgetContents(node);if(id==='current'||id==='next'||id==='notes')renderAll(true)})}refreshModuleMenu();if(persist)saveLayout()}
function showAllWidgets(persist=true){widgets.forEach(node=>node.hidden=false);refreshModuleMenu();if(persist)saveLayout()}
function applyLayout(layout){Object.entries(layout||{}).forEach(([id,r])=>setRect(id,Number(r.x)||0,Number(r.y)||0,Number(r.w)||300,Number(r.h)||200));requestAnimationFrame(()=>renderAll(true))}
function preset(name){showAllWidgets(false);const W=Math.max(900,workspace.clientWidth),H=Math.max(600,workspace.clientHeight),g=12;if(name==='notes'){const leftW=Math.max(380,W*.34),notesX=leftW+g,half=(leftW-3*g)/2;applyLayout({current:{x:g,y:g,w:leftW-2*g,h:H*.27},next:{x:g,y:H*.29,w:leftW-2*g,h:H*.17},controls:{x:g,y:H*.48,w:leftW-2*g,h:H*.16},clock:{x:g,y:H*.66,w:half,h:H*.15},duration:{x:g+half+g,y:H*.66,w:half,h:H*.15},deadline:{x:g,y:H*.83,w:leftW-2*g,h:H*.15},notes:{x:notesX,y:g,w:W-notesX-g,h:H-2*g}});return}if(name==='timer'){applyLayout({current:{x:g,y:g,w:W*.3,h:H*.32},next:{x:g,y:H*.34,w:W*.3,h:H*.22},notes:{x:g,y:H*.58,w:W*.3,h:H*.4-g},clock:{x:W*.32,y:g,w:W*.33,h:H*.43},duration:{x:W*.67,y:g,w:W*.32-g,h:H*.43},deadline:{x:W*.32,y:H*.45,w:W*.33,h:H*.36},controls:{x:W*.67,y:H*.45,w:W*.32-g,h:H*.36}});return}applyLayout({current:{x:g,y:g,w:W*.4,h:H*.43},next:{x:W*.42,y:g,w:W*.25,h:H*.3},notes:{x:W*.42,y:H*.32,w:W*.57-g,h:H*.66-g},controls:{x:g,y:H*.45,w:W*.4,h:H*.18},clock:{x:g,y:H*.65,w:W*.18,h:H*.32-g},duration:{x:W*.2,y:H*.65,w:W*.2,h:H*.32-g},deadline:{x:W*.69,y:g,w:W*.3-g,h:H*.3}})}
let topZ=20;function raise(node){node.style.zIndex=String(++topZ)}
const timerMeasure=document.createElement('canvas').getContext('2d');
function fitTimerNode(node){if(!node||node.hidden)return;const body=node.querySelector('.timer-body'),value=node.querySelector('.timer-value'),sub=node.querySelector('.timer-sub');if(!body||!value)return;const availableW=Math.max(24,value.clientWidth-4),availableH=Math.max(18,value.clientHeight-2),text=value.textContent||'00:00';let sizeByWidth=availableW;if(timerMeasure){timerMeasure.font='690 100px Inter, sans-serif';const measured=Math.max(1,timerMeasure.measureText(text).width);sizeByWidth=availableW*100/measured}const sizeByHeight=availableH/.92,size=Math.max(16,Math.min(sizeByWidth,sizeByHeight)*.97);value.style.fontSize=size+'px';if(sub)sub.style.fontSize=Math.max(9,Math.min(22,Math.max(10,size*.16)))+'px'}
function fitWidgetContents(node){if(node.matches('[data-widget="clock"],[data-widget="duration"],[data-widget="deadline"]'))fitTimerNode(node)}
const resizeEdges=['n','ne','e','se','s','sw','w','nw'];
widgets.forEach(node=>{
  const close=document.createElement('button');close.type='button';close.className='widget-close';close.title='Modul elrejtése';close.setAttribute('aria-label','Modul elrejtése');close.textContent='×';close.onclick=e=>{e.stopPropagation();setWidgetVisible(node.dataset.widget,false)};node.querySelector('.widget-head')?.append(close);
  resizeEdges.forEach(edge=>{const handle=document.createElement('div');handle.className='widget-resizer';handle.dataset.resize=edge;handle.title='Modul méretezése';node.append(handle);handle.addEventListener('pointerdown',e=>{if(e.button!==0)return;e.preventDefault();e.stopPropagation();raise(node);node.classList.add('resizing');const sx=e.clientX,sy=e.clientY,start={x:node.offsetLeft,y:node.offsetTop,w:node.offsetWidth,h:node.offsetHeight},min=widgetMinSize(node.dataset.widget),minW=min.w,minH=min.h;const move=ev=>{const dx=ev.clientX-sx,dy=ev.clientY-sy;let x=start.x,y=start.y,w=start.w,h=start.h;if(edge.includes('e'))w=start.w+dx;if(edge.includes('s'))h=start.h+dy;if(edge.includes('w')){w=start.w-dx;x=start.x+dx}if(edge.includes('n')){h=start.h-dy;y=start.y+dy}if(w<minW){if(edge.includes('w'))x-=minW-w;w=minW}if(h<minH){if(edge.includes('n'))y-=minH-h;h=minH}if(x<0){if(edge.includes('w'))w+=x;x=0}if(y<0){if(edge.includes('n'))h+=y;y=0}if(x+w>workspace.clientWidth)w=Math.max(minW,workspace.clientWidth-x);if(y+h>workspace.clientHeight)h=Math.max(minH,workspace.clientHeight-y);node.style.left=x+'px';node.style.top=y+'px';node.style.width=w+'px';node.style.height=h+'px';if(node.dataset.widget==='current'||node.dataset.widget==='next'||node.dataset.widget==='notes')requestAnimationFrame(()=>renderAll(true));requestAnimationFrame(()=>fitWidgetContents(node))};const up=()=>{node.classList.remove('resizing');removeEventListener('pointermove',move);removeEventListener('pointerup',up);saveLayout();renderAll(true)};addEventListener('pointermove',move);addEventListener('pointerup',up)});});
  node.addEventListener('pointerdown',()=>raise(node),{capture:true});
  const head=node.querySelector('.widget-head');head.addEventListener('pointerdown',e=>{if(e.button!==0||e.target.closest('button,input'))return;e.preventDefault();node.classList.add('dragging');const sx=e.clientX,sy=e.clientY,ox=node.offsetLeft,oy=node.offsetTop;const move=ev=>{const maxX=Math.max(0,workspace.clientWidth-node.offsetWidth),maxY=Math.max(0,workspace.clientHeight-node.offsetHeight);node.style.left=Math.min(maxX,Math.max(0,ox+ev.clientX-sx))+'px';node.style.top=Math.min(maxY,Math.max(0,oy+ev.clientY-sy))+'px'};const up=()=>{node.classList.remove('dragging');removeEventListener('pointermove',move);removeEventListener('pointerup',up);saveLayout()};addEventListener('pointermove',move);addEventListener('pointerup',up)});
  new ResizeObserver(()=>{saveLayout();if(node.dataset.widget==='current'||node.dataset.widget==='next'||node.dataset.widget==='notes')renderAll(true);fitWidgetContents(node)}).observe(node)
});
document.querySelectorAll('[data-preset]').forEach(btn=>btn.onclick=()=>{preset(btn.dataset.preset);saveLayout()});byId('resetLayout').onclick=()=>{try{localStorage.removeItem(layoutKey);localStorage.removeItem(hiddenKey)}catch{}preset('standard');saveLayout()};
modulesButton.addEventListener('pointerdown',e=>e.stopPropagation());modulesButton.onclick=e=>{e.stopPropagation();const r=modulesButton.getBoundingClientRect();modulesMenu.style.left=Math.max(8,Math.min(innerWidth-210,r.left))+'px';modulesMenu.style.top=(r.bottom+5)+'px';modulesMenu.classList.toggle('open')};modulesMenu.addEventListener('pointerdown',e=>e.stopPropagation());addEventListener('pointerdown',()=>modulesMenu.classList.remove('open'));addEventListener('keydown',e=>{if(e.key==='Escape')modulesMenu.classList.remove('open')});
let stored=null,storedHidden=[];try{stored=JSON.parse(localStorage.getItem(layoutKey)||'null');storedHidden=JSON.parse(localStorage.getItem(hiddenKey)||'[]')}catch{}if(stored)applyLayout(stored);else preset('standard');if(Array.isArray(storedHidden))storedHidden.forEach(id=>setWidgetVisible(id,false,false));refreshModuleMenu();addEventListener('resize',()=>{widgets.filter(node=>!node.hidden).forEach(node=>{node.style.left=Math.min(node.offsetLeft,Math.max(0,workspace.clientWidth-node.offsetWidth))+'px';node.style.top=Math.min(node.offsetTop,Math.max(0,workspace.clientHeight-node.offsetHeight))+'px'});renderAll(true)});
// Timers
const pad=n=>String(Math.max(0,Math.floor(n))).padStart(2,'0'),fmt=seconds=>{seconds=Math.max(0,Math.floor(seconds));const h=Math.floor(seconds/3600),m=Math.floor((seconds%3600)/60),s=seconds%60;return h>0?h+':'+pad(m)+':'+pad(s):pad(m)+':'+pad(s)};let durationStartedAt=0,durationElapsed=0,durationRunning=false;
byId('durationStart').onclick=()=>{if(durationRunning){durationElapsed+=(Date.now()-durationStartedAt)/1000;durationRunning=false;byId('durationStart').textContent='Start'}else{durationStartedAt=Date.now();durationRunning=true;byId('durationStart').textContent='Szünet'}};byId('durationReset').onclick=()=>{durationElapsed=0;durationStartedAt=Date.now();durationRunning=false;byId('durationStart').textContent='Start'};byId('deadlineClear').onclick=()=>{byId('deadlineTime').value=''};
function tick(){const now=new Date();byId('clockValue').textContent=pad(now.getHours())+':'+pad(now.getMinutes())+':'+pad(now.getSeconds());const limit=Math.max(60,Number(byId('durationMinutes').value||45)*60),elapsed=durationElapsed+(durationRunning?(Date.now()-durationStartedAt)/1000:0),remaining=limit-elapsed;byId('durationValue').textContent=(remaining>=0?'':'−')+fmt(Math.abs(remaining));byId('durationSub').textContent=fmt(elapsed)+' eltelt';const raw=byId('deadlineTime').value;if(raw){const parts=raw.split(':').map(Number),target=new Date(now);target.setHours(parts[0]||0,parts[1]||0,0,0);const diff=(target-now)/1000;byId('deadlineValue').textContent=(diff>=0?'':'−')+fmt(Math.abs(diff));byId('deadlineSub').textContent=diff>=0?'hátralévő idő':'ennyivel túllépted'}else{byId('deadlineValue').textContent='--:--';byId('deadlineSub').textContent='Nincs határidő'}document.querySelectorAll('[data-widget=\"clock\"],[data-widget=\"duration\"],[data-widget=\"deadline\"]').forEach(fitWidgetContents)}setInterval(tick,250);tick();widgets.forEach(fitWidgetContents);renderAll(true);
})();
</script>
</body>
</html>`;
}
